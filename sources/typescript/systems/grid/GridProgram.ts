import { Shader } from '@cedric-demongivert/gl-tool-shader'
import { ShaderType } from '@cedric-demongivert/gl-tool-shader'
import { Program } from '@cedric-demongivert/gl-tool-shader'

export namespace GridProgram {
  /*export const INSTANCE : Program = new Program()

  INSTANCE.vertex = new Shader(ShaderType.VERTEX)
  INSTANCE.fragment = new Shader(ShaderType.FRAGMENT)

  INSTANCE.vertex.source = require('../../../shaders/grid/grid.vert').default
  INSTANCE.fragment.source = require('../../../shaders/grid/square.frag').default*/
}
