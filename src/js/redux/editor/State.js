import * as openable from '../openable'
import * as selection from '../selection'

export const DEFAULT = {
  entityPanel: openable.State.DEFAULT,
  componentPanel: openable.State.DEFAULT,
  entitySelection: selection.State.DEFAULT
}
